/*
exports.area = function(r) {
  return Math.PI * r * r;
};

exports.circumference = function (r) {
  return 2 * Math.PI * r;
};
모듈(부분)
  # es6 : named export

*/

export var area = r => Math.PI*r*r;